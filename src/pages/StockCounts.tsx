import { ClipboardList } from "lucide-react";
import ComingSoon from "./ComingSoon";

const StockCounts = () => {
  return <ComingSoon title="Stock Counts" icon={<ClipboardList className="h-16 w-16" />} />;
};

export default StockCounts;
